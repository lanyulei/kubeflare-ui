import ClusterResourcePage from '../../resource';

const ClusterNodes = () => (
  <ClusterResourcePage
    titleId="menu.cluster.node.nodes"
    defaultTitle="集群节点"
  />
);

export default ClusterNodes;
