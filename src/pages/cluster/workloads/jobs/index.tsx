import ClusterResourcePage from '../../resource';

const Jobs = () => (
  <ClusterResourcePage
    titleId="menu.cluster.clusterWorkloads.clusterWorkloadsJobs"
    defaultTitle="任务"
  />
);

export default Jobs;
