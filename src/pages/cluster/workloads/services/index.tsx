import ClusterResourcePage from '../../resource';

const Services = () => (
  <ClusterResourcePage
    titleId="menu.cluster.workloads.services"
    defaultTitle="服务"
  />
);

export default Services;
