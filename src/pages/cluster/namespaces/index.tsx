import ClusterResourcePage from '../resource';

const Namespaces = () => (
  <ClusterResourcePage
    titleId="menu.cluster.clusterNamespaces"
    defaultTitle="命名空间"
  />
);

export default Namespaces;
